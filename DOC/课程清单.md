# 课程大纲
1. 设置flybird的环境
2. 配置DQN网络
3. 经验回放和超参数
4. 实现Epsilon贪心算法
5. 实现目标网络和策略网络
6. 损失夯实和网络优化
7. 代码优化
8. 在cartPole上测试DQN
9. 在flappybird 上训练
10. Double DQN实现
11. Deuling DQN 实现


## 1. 设置flybird的环境

    1. 安装虚拟环境 DQN
    2. 安装包 
        安装pytorch 环境包  ,推荐安装GPU版本，
        pip install gymnasium flappy-bird-gymnasium 
    3. 查看开关“lidar”的差异，关闭lidar 有12个参数，将这些参数和GitHub上的对比一下  
        查看变量：动作、环境状态、奖励



## 2. 配置DQN网络
    1. 初始化DQN网络，init和forward 两个部分
    2. 查看网络结构（原版的只有两层，添加到3或4层增加复杂度）
    3. 输入随机数给到网络，查看网络输出内容
    4. 添加网络到agent代码中
什么是网络，网络的输入和输出，中间层，DQN的基本逻辑，如何替代的Q网络
pytorch 安装包，GPU和CPU的版本
建立好模型结构后，写main函数验证模型输入输出，包括取值范围，入和出的数量
设断点，查看state的数值，查看state。shape的大小
批量计算---如10组输入，查看state和state。shape。
添加DQN到agent，模型policy DQN,



## 3. 经验回放和超参数
    1. 创建经验回放代码 experience replay.py
    2. 创建超参数代码 hyperparameters.py
    3. 添加 __name__ 函数

## 4. 实现Epsilon贪心算法
    随机概率 epsilon，随循环的次数而缩小。但不小于最小值

## 5. 实现目标网络和策略网络
    创建策略网络policy_dqn，目标网络target_dqn
    存储数据 mini batch，数据存到足够才开始训练
    每间隔network_sync_rate的训练次数，同步策略网络和目标网络

    optimize 策略优化函数
    奖励的计算：
    target_q = reward + self.discount_factor_g * target_dqn(new_state).max() 
    current_q_value = policy_dqn(state)
    算出当前的佳慧，和未来期望的价值，计算loss
    loss =  self.loss_function(current_q_value, target_q)


## 6. 损失函数和网络优化


## 7. 代码优化
    optimize 函数中优化计算效率，将单个计算的换成一组一组计算的
    其中调用zip()将数据压缩到一起，torch.stack、torch.tensor转换数据格式，需要注意数据格式转换不要出错

## 8. 在cartPole上测试DQN
    1. 代码中添加log信息，保存模型，
    2. 编写lunch.jison 文件，配置不同的调试模式，来实现功能
    3. 添加main函数中的信息，添加命令行参数解析


## 9. 在flappybird 上训练

    编写lunch.jison 文件，配置不同的调试模式，来实现功能 

## 10. Double DQN实现
    enable_double_dqn，修改了 optimize函数中计算target_q的部分
            if self.enable_double_dqn:
                best_actions_from_policy = policy_dqn(new_states).argmax(dim=1)

                target_q = rewards + (1-terminations) * self.discount_factor_g * \
                                target_dqn(new_states).gather(dim=1, index=best_actions_from_policy.unsqueeze(dim=1)).squeeze()
            else:
                # Calculate target Q values (expected returns)
                target_q = rewards + (1-terminations) * self.discount_factor_g * target_dqn(new_states).max(dim=1)[0]


## 11. Deuling DQN 实现

    enable_dueling_dqn，主要修改了网络的部分